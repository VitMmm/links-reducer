# Reducer Link - Сервис для укорачивания ссылок

Сервис для создания коротких ссылок с возможностью работы между устройствами.

## Установка

1. Установите зависимости:
```bash
npm install
```

## Запуск

```bash
npm start
```

Сервер запустится на порту 3000 (или на порту, указанном в переменной окружения PORT).

Откройте в браузере: `http://localhost:3000`

## Использование

1. Введите длинную ссылку в поле "Введите длинную ссылку"
2. (Опционально) Укажите свой суффикс в поле "Суффикс для короткой ссылки"
3. Нажмите "Создать короткую ссылку"
4. Скопируйте полученную короткую ссылку

## API

### POST /api/links
Создание короткой ссылки

**Тело запроса:**
```json
{
  "longUrl": "https://example.com/very/long/url",
  "slug": "my-custom-slug" // опционально
}
```

**Ответ:**
```json
{
  "success": true,
  "slug": "my-custom-slug",
  "shortUrl": "http://localhost:3000/#my-custom-slug",
  "longUrl": "https://example.com/very/long/url"
}
```

### GET /api/links/:slug
Получение длинной ссылки по slug

**Ответ:**
```json
{
  "success": true,
  "slug": "my-custom-slug",
  "longUrl": "https://example.com/very/long/url"
}
```

## База данных

Используется SQLite база данных `links.db`, которая создается автоматически при первом запуске.

## Деплой на сервер

### ⚠️ Важно: GitHub Pages не поддерживает Node.js

GitHub Pages - это статический хостинг, который может отдавать только HTML/CSS/JS файлы, но **не может выполнять серверный код** (Node.js).

**Для работы между устройствами нужен сервер с Node.js!**

### Варианты деплоя:

**Все варианты (в т.ч. без Fly.io):** [DEPLOY_ALTERNATIVES.md](./DEPLOY_ALTERNATIVES.md)

#### 🎯 Самый простой: Railway (без CLI)
Подключили GitHub → нажали Deploy в браузере. Никакого Fly CLI.
- [railway.app](https://railway.app) → New Project → Deploy from GitHub repo
- См. [DEPLOY.md](./DEPLOY.md)

#### 🚀 Fly.io + GitHub
Автоматический деплой через GitHub Actions. Нужна установка Fly CLI.
- См. [DEPLOY_FLY.md](./DEPLOY_FLY.md)

#### 🔀 Гибридный вариант: GitHub Pages + Railway
- GitHub Pages для фронтенда (бесплатно)
- Railway для backend API (бесплатно)
- Нужно настроить CORS и указать URL backend в `index.html`
См. подробную инструкцию в [DEPLOY.md](./DEPLOY.md)

### Другие варианты:

#### 1. Railway (рекомендуется - бесплатно)
1. Зарегистрируйтесь на [railway.app](https://railway.app)
2. Создайте новый проект
3. Подключите GitHub репозиторий или загрузите код
4. Railway автоматически определит Node.js и запустит сервер

#### 2. Render (бесплатно)
1. Зарегистрируйтесь на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий
4. Укажите команду запуска: `npm start`
5. Укажите порт: `3000`

#### 3. Собственный сервер (VPS) ⭐
Если у вас есть свой сервер с SSH доступом, см. подробную инструкцию в [DEPLOY_VPS.md](./DEPLOY_VPS.md)

**Краткая версия:**
```bash
# На сервере
git clone <ваш-репозиторий>
cd reducer
npm install
npm install -g pm2
pm2 start server.js --name reducer
pm2 save
pm2 startup
```

Для настройки домена и Nginx см. полную инструкцию в `DEPLOY_VPS.md`

#### 4. Heroku
1. Установите Heroku CLI
2. `heroku create`
3. `git push heroku main`

### Переменные окружения

При необходимости можно задать порт через переменную окружения:
```bash
PORT=3000 npm start
```

## Особенности

- Работает между устройствами и браузерами
- Данные хранятся в базе данных SQLite
- Поддержка пользовательских суффиксов
- Автоматическая генерация суффиксов
- Валидация URL

**Важно:** Для работы между устройствами обязательно нужен сервер с поддержкой Node.js. Простой FTP-хостинг не подойдет.
